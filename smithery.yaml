# Smithery.ai configuration
startCommand:
  type: stdio
  configSchema: {}
  commandFunction: |-
    (config) => ({
      command: "python",
      args: [
        "main.py"
      ],
      env: {}
    })
tools:
  - query_technique
  - search_technique_full
  - list_tactics
