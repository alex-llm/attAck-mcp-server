# Smithery.ai configuration for container-based deployments
runtime: container
build:
  dockerfile: Dockerfile
  dockerBuildPath: .
startCommand:
  type: http
tools:
  - query_technique
  - search_technique_full
  - list_tactics
